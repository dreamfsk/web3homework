server:
  port: "8080"
  host: "0.0.0.0"
  mode: "debug"  # debug, release, test

ctxkey: "operator"
dbtype: "mysql"

sqlite:
  dbname: "blog.sqlite.db"
  migrate: false

mysql:
  host: "127.0.0.1"
  port: "3306"
  username: "blog"
  password: "123456"
  dbname: "blog"
  migrate: false

jwt:
  secret: "your-secret-key-change-in-production"
  expire: "24h"

language:
  local: "zh-cn"

# zap logger configuration
zap:
  level: info
  format: console
  prefix: "[github.com/dreamfsk/blog]"
  director: log
  show-line: true
  encode-level: LowercaseColorLevelEncoder
  stacktrace-key: stacktrace
  log-in-console: true
  retention-day: -1